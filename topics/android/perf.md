模块化

`systrace` for 
 render time, see what is blocking

`Layout Inspector`
to see redundant drawing and app hierarchy

`Android Profiler`
to see traces

`Dynamic Feature Module`
To download modules only when on use